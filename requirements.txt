# Local host deps — data prep + report only. No GPU/training deps here;
# the Colab notebook installs its own (Unsloth/PEFT/TRL/evalplus) in its first cell,
# pinned to whatever CUDA/torch build Colab provides that session.
datasets>=2.19
huggingface_hub>=0.23
datasketch>=1.6
pandas>=2.2
pyyaml>=6.0
tqdm>=4.66
